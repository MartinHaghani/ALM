server {
    root /opt/open_mower_ros/web;
    listen       8080;

    location ~* \.wasm$ {
        default_type application/wasm;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
